using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Mantap : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("gg");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
